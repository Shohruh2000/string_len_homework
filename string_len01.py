def main(a):
    """
    A variable of type string is given. Find its length.
    Args:
        a: string
    Returns:
        length of string
    """
    
    return a
a = main("python")
print(len(a))
